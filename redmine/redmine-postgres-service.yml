apiVersion: v1
kind: Service
metadata:
  name: redmine-postgres-service
  labels: 
    app: redmine-postgres
spec:
  type: NodePort
  selector:
    app: redmine-postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      nodePort: 32749